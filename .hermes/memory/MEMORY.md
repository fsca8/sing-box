gomobile/libbox 调试(Windows/MSYS): MSYS bash 滤掉cmd.exe隐藏变量(=C:=...), 干净终端复现不了malformed env var panic——用原生Go launcher(c.Env=append([]string{`=::=::\`},os.Environ()...))注入绕过; 复现/验证须用真实包(./experimental/libbox); 原生go吃不了/tmp路径须cygpath -w; gomobile须显式-androidapi 23。详见framework-pitfalls.md
§
singbird Windows 排查日志地图: 后端自身日志 singbox.log 在 exe 所在目录(cwd: dev Release 或安装目录 C:\Programs\singbird), 不在数据目录; 控制器 ctrl 日志在 %TEMP%\singbird-YYYYMMDD.log(无连字符, AppLogger 轮转保留3); 数据目录=%APPDATA%\io.nekohasekai.sfm\<ProductName>(由 windows/runner/Runner.rc 决定)——更名即换目录旧配置'消失'; 后端 exe 名 sing-box-netbird-v<tag>-<hash>-<date> 可精确对 commit; run.log 只有 started 无 exited = singbox_controller._closeLogFile 读字段而非捕获局部变量的竞态(unawaited close 会关掉新 sink 并置 null, 修法: 先 final sink=_logFileSink 再置 null)
§
MSYS/Inno 打包坑: 原生exe不吃/c/路径先cd或cygpath -w; pipefail下ls失败静默exit2, 多候选用for+[ -f ]; winget InnoSetup per-user, ISCC在%LOCALAPPDATA%\Programs\Inno Setup 6\; ChineseSimplified.isl从jrsoftware/issrc Files/Languages/vendor(非Unofficial); 7-Zip打不开Inno 6.7.3产物, 验证靠ISCC Compressing清单+grep "Inno Setup Setup Data"+VersionInfo; ISCC Error32=输出exe被锁(tasklist/Defender,重试); 静默安装-Verb RunAs ps1一次UAC装→校验→卸
§
singbird双引擎共存流量(Windows, 源码核实): sing-box tun auto_route metric压0挂0.0.0.0/0(tun_windows.go:134), DNS fakeip劫持; netbird只加管理下发网段路由metric=1(systemops_windows.go:274 iphelper), DNS用注册表DnsPolicyConfig域名匹配(host_windows.go), 默认不碰默认路由(除非exit node); 最长前缀匹配互不干扰; 双默认路由时sing-box metric0胜→exit node须sing-box规则显式放行
§
配置双文件profiles.json+sing-box-config.json都改base64写回; adb pull须C:/绝对; 大量error=单目标高频超时=RustDesk自动推导API端口=ID端口-2(21116→21114),OSS hbbs/hbbr不监听21114→SYN无应答,21116注册正常,非代理问题
§
netbird Android<12修复(2026-08-10实测推翻初判): 初判"Go1.25内置ignoreSIGSYS保护已足够"→实测MIPAD4/Android8.1闪退(SIGSYS pidfd_open自身PID,x8=0x1b2),Go1.25保护在gomobile构建下未兜住,勿依赖; 真因=上游experimental/libbox/pidfd_android.go(go:linkname覆写os.checkPidfdOnce返回ErrInvalid)被netbird merge(f74c294c8)误删; 修复=git show testing:experimental/libbox/pidfd_android.go恢复+rebuild-libbox.sh重编aar, -checklinkname=0须保留; BoxService.kt的SDK_INT<31跳过是过时防御已移除(只改Kotlin不恢复Go workaround照样崩); 验证=dex无skipping字符串+so有pidfd_android符号; 坑=rebuild-libbox.sh的NDK路径须Windows原生C:/(MSYS /c/报platforms.json找不到)