gomobile/libbox 调试(Windows/MSYS): MSYS bash 启动时滤掉 cmd.exe 隐藏变量(=C:=.../=::=::\), 干净终端复现不了 gomobile 的 malformed env var panic——用原生 Go launcher(c.Env=append([]string{`=::=::\`},os.Environ()...)) 注入 env block 绕过清洗稳定复现; env 二次校验在 gobind 之后, 复现/验证须用真实包(./experimental/libbox), bogus 包或裸最小模块到不了该路径; 原生 go 工具吃不了 /tmp/... 路径(报 malformed import path), 用 cygpath -w 或相对路径; gomobile 默认 androidapi=16 撞 NDK r29 的 21..35 检查, 须显式 -androidapi 23。详见 .hermes/skills/sing-box-flutter/references/framework-pitfalls.md 的 Windows/MSYS 段。
§
singbird Inno Setup 打包经验(2026-08): winget 装 JRSoftware.InnoSetup 是 per-user, ISCC 在 %LOCALAPPDATA%\Programs\Inno Setup 6\; ISCC 原生 exe 不吃 MSYS 路径(传参须 cygpath -w); pipefail 下 `ls 多路径|head -1` 任一失败静默 exit2, 探测路径用 [ -f ] 循环; ChineseSimplified.isl 社区翻译不捆绑, 从 jrsoftware/issrc 的 Files/Languages/(非 Unofficial, 那路径404) 下载 vendor 到 installer/languages/; 7-Zip 打不开 Inno 6.7.3 产物, 内容验证靠 ISCC 编译日志 Compressing 清单 + grep "Inno Setup Setup Data" + VersionInfo; Error32 输出锁 = setup.exe 进程在跑(tasklist 确认, 别乱 kill); 静默安装实测用 -Verb RunAs ps1 一次 UAC 装→校验→卸。
§
singbird Windows 排查日志地图: 后端自身日志 singbox.log 在 exe 所在目录(cwd: dev Release 或安装目录 C:\Programs\singbird), 不在数据目录; 控制器 ctrl 日志在 %TEMP%\singbird-YYYYMMDD.log(无连字符, AppLogger 轮转保留3); 数据目录=%APPDATA%\io.nekohasekai.sfm\<ProductName>(由 windows/runner/Runner.rc 决定)——更名即换目录旧配置'消失'; 后端 exe 名 sing-box-netbird-v<tag>-<hash>-<date> 可精确对 commit; run.log 只有 started 无 exited = singbox_controller._closeLogFile 读字段而非捕获局部变量的竞态(unawaited close 会关掉新 sink 并置 null, 修法: 先 final sink=_logFileSink 再置 null)
§
MSYS 原生工具路径坑: git -C /c/... 报 cannot change to、ISCC/go build 同理——先 cd 或用 cygpath -w 转 Windows 路径; set -o pipefail 下 'ls a b 2>/dev/null | head -1' 任一 ls 失败整条管道静默 exit2, 多候选路径用 for+[ -f ] 循环。Inno 打包坑: 7-Zip 打不开 Inno 6.7.3 产物(核对包内容靠 ISCC 编译日志 Compressing 列表); ChineseSimplified.isl 是社区翻译 Inno 不捆绑需从 issrc 仓库 Files/Languages/ vendor; 输出 exe 可能被 Defender 瞬时锁定(ISCC Error32 重试即可)